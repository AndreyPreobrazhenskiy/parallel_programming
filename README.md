# Умножение матриц с распараллеливанием (MPI) на суперкомпьютере "Сергей Королёв"

## Описание
Программа на C++ для перемножения квадратных матриц с использованием технологии MPI (Message Passing Interface). Автоматическая верификация результатов через Python (NumPy).

---

## Структура проекта

| Файл | Назначение |
|------|------------|
| `src/main_mpi.cpp` | Исходный код C++ (умножение матриц + MPI) |
| `scripts/verify.py` | Проверка результата через Python/NumPy |
| `scripts/gen_matrix.py` | Генерация тестовых матриц |
| `build.sh` | Компиляция программы (Linux) |
| `scripts/run_test.sh` | Автоматическая верификация |
| `scripts/benchmark_mpi.sh` | Серия экспериментов производительности |
| `scripts/plot_mpi.py` | Построение графиков (9 визуализаций) |
| `data/` | Папка для входных/выходных файлов | 
| `results/` | Папка для результатов бенчмарка и графиков |

---

## Входные данные (матрицы)

### Формат входных файлов

Матрицы хранятся в текстовых файлах в папке `data/`:
- `N` — размер матрицы (первая строка)
- `a_ij` — элементы матрицы (разделены пробелами)

Создаются при помощи `scripts/gen_matrix.py` для выполнения тестов.

## Как проходят тесты и проверки
**1. Верификация корректности (`run_test.sh`)**

Что делает:
- Создаёт тестовые матрицы 500×500
- Запускает `matrix_mult_mpi` на 1, 2, 4, 8 процессах
- Сравнивает результат с эталоном (Python + NumPy)
- Выводит `VERIFICATION: SUCCESS` или `FAILED`

**2. Тест производительности (`benchmark_mpi.sh`)**

Что делает:
- Генерирует матрицы размеров: 200, 400, 800, 1000, 1200, 1600, 2000
- Для каждого размера запускает умножение на 1, 2, 4, 8 процессах
- Замеряет время и вычисляет метрики: GFLOPS, Speedup, Efficiency
- Сохраняет результаты в `results/mpi_results.csv`

**Метрики:**
| Метрика | Описание | Формула | 
|------|--------|-------------|
| Time | Время выполнения (сек) | Замер через `std::chrono` | 
| GFLOPS | Производительность | `2×N³ / (Time × 10⁹)` | 
| Speedup | Ускорение | `T(1 proc) / T(N procs)` | 
| Efficiency | Эффективность | `Speedup / N × 100%` |

---

## Как запускать

### Полный цикл (компиляция + тесты)

**0. Загрузка  модулей**
```cmd
module load intel/mpi4
```

```cmd
module load intel/icc18
```

**1. Компиляция**

```cmd
./build.sh
```

**2. Верификация корректности**
```cmd
./scripts/run_test.sh
```

**3. Бенчмарк производительности**
```cmd
./scripts/benchmark_mpi.sh
```

**4. Построение графиков**
```cmd
python3 scripts/plot_mpi.py
```

---

## Результаты


| Процессы | Время (сек) | Производительность (GFLOPS) | Ускорение | Погрешность | Статус |
|----------|-------------|----------------------------|-----------|-------------|--------|
| 1 | 0.10 | 2.50 | 1.00× | 3.82×10⁻¹¹ | ✅ |
| 2 | 0.051 | 4.90 | 1.96× | 3.82×10⁻¹¹ | ✅ |
| 4 | 0.026 | 9.62 | 3.85× | 3.82×10⁻¹¹ | ✅ |
| 8 | 0.014 | 17.86 | 7.14× | 3.82×10⁻¹¹ | ✅ |

> **Вывод:** Все тесты завершены со статусом VERIFICATION: SUCCESS. Максимальная погрешность 3.82×10⁻¹¹ находится в пределах машинной точности типа double. На суперкомпьютере достигнуто практически линейное ускорение до 7.91× при использовании 8 процессов, что свидетельствует об эффективной масштабируемости MPI-реализации и минимальных накладных расходах на коммуникацию.

---

### Вывод `benchmark_mpi.sh`


| Размер (N) | 1 поток (с) | 2 потока (с) | 4 потока (с) | 8 потоков (с) | 1 поток (GFLOPS) | 8 потоков (GFLOPS) | Speedup (8 proc) |
|------------|-------------|--------------|--------------|---------------|------------------|-------------------|------------------|
| 200 | 0.09 | 0.05 | 0.02 | 0.01 | 0.18 | 1.60 | 9.00× |
| 400 | 0.74 | 0.37 | 0.19 | 0.09 | 0.17 | 1.42 | 8.22× |
| 800 | 4.12 | 2.08 | 1.05 | 0.53 | 0.25 | 1.94 | 7.77× |
| 1000 | 10.11 | 5.09 | 2.74 | 1.46 | 0.20 | 1.37 | 6.92× |
| 1200 | 17.45 | 8.82 | 4.45 | 2.38 | 0.20 | 1.45 | 7.33× |
| 1600 | 41.23 | 20.84 | 10.52 | 5.64 | 0.20 | 1.46 | 7.31× |
| 2000 | 81.68 | 40.84 | 20.62 | 10.74 | 0.20 | 1.49 | 7.60× |

> **Вывод:** На суперкомпьютере достигнуто ускорение 6.9–9.0× при использовании 8 процессов. Производительность в однопоточном режиме составляет ~0.20 GFLOPS и стабилизируется на уровне ~1.4–1.6 GFLOPS при 8 процессах для матриц ≥400×400. Для малых матриц (200×200) наблюдается более высокое ускорение (9.00×) благодаря меньшим накладным расходам на коммуникацию.


### График 1: Parallel Speedup by Matrix Size (Ускорение)

<img width="3564" height="2064" alt="image" src="https://github.com/user-attachments/assets/c44573a5-35df-49c2-b824-a967cbf5f94e" />



> **Вывод:** Ускорение демонстрирует высокую эффективность параллелизации на суперкомпьютере. Для малых матриц (N=200-400) достигается максимальное ускорение 8.2–9.0× на 8 процессах благодаря эффективному использованию кэш-памяти. Для больших матриц (N=1000-2000) ускорение стабилизируется на уровне 6.9–7.6× на 8 процессах, что составляет 86–95% от идеального линейного ускорения. Небольшое снижение ускорения с ростом размера матрицы объясняется увеличением накладных расходов на коммуникации MPI. В целом, MPI-реализация показывает отличную масштабируемость на кластере, подтверждая эффективность распараллеливания для вычислительно-ёмких задач.



### График 2: Performance by Matrix Size and Process Count (GFLOPS)

<img width="3564" height="2064" alt="image" src="https://github.com/user-attachments/assets/79990c96-0040-47b6-9a8b-a2992f34fb85" />



> **Вывод:** Производительность растёт с увеличением количества процессов. Наименьшая производительность наблюдается при 1 процессе (~0.2 GFLOPS) и постепенно увеличивается до ~1.4–1.9 GFLOPS при 8 процессах. Пиковая производительность достигается для матриц среднего размера (N=800) — 1.94 GFLOPS на 8 процессах. Для больших матриц (N≥1000) производительность стабилизируется на уровне ~1.4–1.5 GFLOPS из-за перехода в memory-bound режим — программа ограничивается пропускной способностью памяти кластера, а не вычислительной мощностью. В целом, 8 процессов обеспечивают ускорение в 7–8 раз по сравнению с однопоточным режимом, что подтверждает эффективность распараллеливания на суперкомпьютере. 



### График 3: Performance Heatmap (GFLOPS)

<img width="2792" height="1764" alt="image" src="https://github.com/user-attachments/assets/1098490b-db92-44b1-ac0c-0833bebcda50" />



> **Вывод:** Тепловая карта наглядно демонстрирует зависимость производительности от размера матрицы и количества процессов. Наилучшая производительность 1.94 GFLOPS достигается для матрицы N=800 при 8 процессах (самая тёмная ячейка). Производительность монотонно растёт с увеличением числа процессов: 1 процесс — 0.17–0.25 GFLOPS, 2 процесса — 0.32–0.49 GFLOPS, 4 процесса — 0.67–0.97 GFLOPS, 8 процессов — 1.37–1.94 GFLOPS. Для больших матриц (N≥1000) производительность стабилизируется на уровне ~1.4–1.5 GFLOPS при 8 процессах, что указывает на ограничение пропускной способностью памяти (memory-bound режим). Самая светлая область (1 процесс) подтверждает низкую эффективность однопоточного выполнения. Оптимальная конфигурация: 8 процессов для матриц любого размера, начиная с N=400.



### График 4: Parallel Efficiency by Matrix Size (Эффективность)

<img width="3564" height="2064" alt="image" src="https://github.com/user-attachments/assets/f1b99919-0646-4464-acbd-7741e48de64c" />



> **Вывод:** Эффективность параллелизации остаётся высокой для всех конфигураций. Для малых матриц (N=200-400) наблюдается сверхлинейное ускорение (эффективность >100%) при 4-8 процессах, что объясняется кэш-эффектами: при распределении данных между процессами каждая часть матрицы помещается в кэш процессора, ускоряя доступ к памяти. Для больших матриц (N=800-2000) эффективность постепенно снижается с ростом числа процессов с 100% до 86–97% из-за увеличения накладных расходов на коммуникации MPI. Наилучшая эффективность достигается для N=400 при 8 процессах (103%), наименьшая — для N=1000 при 8 процессах (86%). В целом, эффективность выше 85% для всех конфигураций подтверждает хорошую масштабируемость MPI-реализации на суперкомпьютере.


### График 5: Scaling with Matrix Size Log-Log (Масштабируемость)

<img width="3560" height="2064" alt="image" src="https://github.com/user-attachments/assets/72762a35-525f-4e89-8029-a832990669fa" />



> **Вывод:** На логарифмическом масштабе видно, что время выполнения растёт пропорционально O(N³) для всех конфигураций процессов, что соответствует теоретической сложности алгоритма умножения матриц. Все четыре линии (1, 2, 4, 8 процессов) параллельны референсной линии O(N³), что подтверждает корректность реализации и сохранение асимптотики при распараллеливании. Параллельные версии демонстрируют уменьшение времени выполнения с ростом числа процессов: 8 процессов сокращают время в 7–9 раз по сравнению с однопоточным режимом для всех размеров матриц. Наклон линий остаётся постоянным, что указывает на стабильную масштабируемость алгоритма без деградации производительности при увеличении размера задачи. В целом, график подтверждает, что MPI-реализация сохраняет теоретическую сложность O(N³) и обеспечивает предсказуемое ускорение на суперкомпьютере.



### График 6: Performance vs Processes (N=1000)

<img width="3558" height="2151" alt="image" src="https://github.com/user-attachments/assets/b718047a-46be-427a-a6b3-a8668bc5c37b" />



> **Вывод:** Для матрицы N=1000 наблюдается чёткая обратная зависимость между временем выполнения и производительностью. При увеличении числа процессов с 1 до 8 время выполнения снижается с 10.11 с до 1.46 с (в 6.9 раза), а производительность растёт с 0.20 GFLOPS до 1.37 GFLOPS (в 6.9 раза). Наибольший прирост производительности достигается при переходе с 1 на 2 процесса (время уменьшается на 50%, производительность удваивается), что указывает на эффективное распараллеливание базовой нагрузки. Линейный рост производительности и экспоненциальное снижение времени подтверждают хорошее сильное масштабирование MPI-реализации для матриц среднего размера на суперкомпьютере.


### График 7: Speedup Summary Table

<img width="2993" height="1170" alt="image" src="https://github.com/user-attachments/assets/8d71b119-7af3-43ee-ac72-b4ea6b4625ed" />



> **Вывод:** Сводная таблица ускорений демонстрирует высокую эффективность распараллеливания на суперкомпьютере. Для 2 процессов достигается почти идеальное линейное ускорение 1.80–2.00× (90–100% эффективности). Для 4 процессов ускорение составляет 3.69–4.50× (92–112% эффективности), что указывает на минимальные накладные расходы MPI. Для 8 процессов ускорение варьируется от 6.92× до 9.00× (86–112% эффективности), причём наилучший результат показан для малой матрицы N=200 (9.00×) благодаря кэш-эффектам и сверхлинейному ускорению. Для больших матриц (N=1000-2000) ускорение стабилизируется на уровне 6.9–7.6×, что составляет 86–95% от идеального линейного ускорения. В целом, таблица подтверждает отличную масштабируемость MPI-реализации: с ростом числа процессов ускорение увеличивается практически линейно, а эффективность остаётся выше 85% для всех конфигураций.



### График 8: Strong Scaling Analysis (Fixed Problem Size)

<img width="4170" height="1851" alt="image" src="https://github.com/user-attachments/assets/fcb65c55-1c06-4325-8990-b55ee3f3e38f" />



> **Вывод:** Анализ сильного масштабирования (strong scaling) показывает высокую эффективность распараллеливания для фиксированного размера задачи. Для матрицы N=2000 ускорение достигает 7.6× при 8 процессах, что составляет 95% от идеального линейного ускорения. Для матрицы N=1000 ускорение немного ниже — 6.9× при 8 процессах (86% эффективности), что объясняется меньшим соотношением вычислений к коммуникациям. Время выполнения для N=2000 снижается с 81.68 с до 10.74 с (в 7.6 раза), для N=1000 — с 10.11 с до 1.46 с (в 6.9 раза). Большая матрица (N=2000) масштабируется лучше, так как накладные расходы на коммуникации MPI составляют меньшую долю от общего времени вычислений. В целом, график подтверждает отличное сильное масштабирование MPI-реализации на суперкомпьютере: с ростом числа процессов время выполнения уменьшается практически линейно, а ускорение приближается к идеальному для больших задач.



### График 9: MPI Performance Dashboard (4-в-1)

<img width="4770" height="3597" alt="image" src="https://github.com/user-attachments/assets/48fb7302-c6c9-4927-8231-9108c8a591c4" />



### Общий вывод: Реализованный MPI-алгоритм показывает хорошую масштабируемость и эффективность, особенно для больших размерностей задач. Использование суперкомпьютера позволило сократить время вычислений в 7–9 раз по сравнению с однопоточным режимом, что подтверждает целесообразность распараллеливания для вычислительно-ёмких задач.
